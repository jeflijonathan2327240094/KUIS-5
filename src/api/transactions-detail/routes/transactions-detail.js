'use strict';

/**
 * transactions-detail router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::transactions-detail.transactions-detail');
